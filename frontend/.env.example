VITE_API_URL=http://localhost:8000/api

# Production (Cloudflare Pages): set VITE_API_URL=/api in the Pages build env.
# This makes all /api/* calls same-origin so Safari/WebKit accepts session cookies.
# Also set the Pages runtime var: BACKEND_ORIGIN=https://api.35.239.68.226.sslip.io
